from setuptools import setup

setup(
    name='your_project_name',
    version='1.0',
    install_requires=[
        # List of your dependencies here, if any
    ],
    scripts=['nltk_download.py'],
)
